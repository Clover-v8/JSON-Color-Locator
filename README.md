# JSON-Color-Locator
Universal JSON Color Locator is a clean, dependency-free web tool for inspecting and refactoring color values in JSON files. It recursively scans complex structures, visualizes detected colors, supports precise path-based replacements with undo history, and outputs a formatted, ready-to-use JSON result
